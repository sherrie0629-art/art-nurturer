## 问题
桃花扇容器用了 `-ml-16`（左移 64px），而 "执扇问心" 文字只用了 `-ml-6`（左移 24px），两者偏移量差 40px，所以文字并不在扇子正下方，看起来偏右。

## 修改
`src/components/FortuneRippleScene.tsx` 第 267 行：

- 把文字的 `-ml-6` 改为 `-ml-16`，与扇子容器对齐
- 保留 `pl-[0.35em]` 用来抵消 `tracking-[0.35em]` 末字多出的字距，使视觉中心对齐扇子中心

## 验证
改完后用 Playwright 截图 `/daily-fortune-stick`，裁剪扇子+文字区域，确认 "执扇问心" 的视觉中心正好落在扇轴正下方。